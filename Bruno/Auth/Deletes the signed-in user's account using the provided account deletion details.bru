meta {
  name: Deletes the signed-in user's account using the provided account deletion details.
  type: http
  seq: 9
  tags: [
    Auth
  ]
}

delete {
  url: {{baseUrl}}/Auth/deleteAccount
  body: json
  auth: inherit
}

body:json {
  {
    "currentPassword": ""
  }
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/Auth/deleteAccount
    method: DELETE
    mode: json
    body:json: {
      {
        "currentPassword": ""
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
