meta {
  name: Returns the avatar image for the specified user or the currently signed-in user. (GET)
  type: http
  seq: 5
  tags: [
    Images
  ]
}

get {
  url: {{baseUrl}}/Images/Avatar/:userId
  body: none
  auth: inherit
}

params:path {
  userId: 
}

example {
  name: 200 Response
  description: OK
  
  request: {
    url: {{baseUrl}}/Images/Avatar/:userId
    method: GET
    mode: none
    params:path: {
      userId: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
